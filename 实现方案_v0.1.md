# 结构化需求驱动的数据采集项目实现方案 v0.1

## 1. 文档目标

本文档用于把 [需求文档_v0.1](D:\wsq\projects\db-output\需求文档_v0.1.md) 转化为可执行的第一版实现方案。

目标是得到一个适合个人或小团队维护的、本地可运行的、规则驱动的定向采集系统。第一版重点保证：

- 架构稳定
- 接口可扩展
- 数据流可追溯
- 输出结果容易验证
- 后续可平滑扩展到 Web、MCP、skill 和更多数据库输出

## 2. 第一版技术选型

### 2.1 语言与运行环境

建议：

- Python 3.12

理由：

- 生态成熟，适合网络采集、文本清洗、SQLite、CLI
- 对个人和小团队维护成本低
- 后续接 Web、MCP、数据处理都顺手

### 2.2 依赖建议

建议尽量克制，不引入过重框架。

核心依赖建议：

- `pydantic`：任务输入、配置、领域对象建模
- `httpx`：统一 HTTP 客户端
- `selectolax`：HTML 解析，速度快，适合采集场景
- `readability-lxml` 或自定义正文提取模块：正文抽取
- `typer`：CLI
- `rich`：CLI 输出与日志可读性
- `orjson`：高性能 JSON 序列化
- `tenacity` 或自定义 retry：重试策略
- `aiosqlite` 或标准库 `sqlite3`：SQLite 输出
- `python-dateutil`：时间解析
- `rapidfuzz`：轻量相似度与去重辅助
- `pyyaml`：配置文件支持
- `zstandard` 或 `gzip`：压缩存储

第一版不建议默认引入：

- Celery
- Kafka
- Spark
- Playwright 作为默认路径
- SQLAlchemy 这类重量级 ORM

理由：

- 第一版以可控和简单为主
- SQLite 表结构明确，直接 SQL 更容易看清数据写入行为
- 浏览器抓取先留接口，不作为默认依赖

### 2.3 并发模型

建议：

- 采集层使用 `asyncio + httpx.AsyncClient`
- 解析、清洗和写文件可同步或轻量异步

理由：

- 网络请求是主要等待点
- 对当前体量，不需要多进程复杂调度

## 3. 总体架构

建议采用六层架构：

1. `interfaces`
- CLI 入口
- 后续 Web、MCP 入口

2. `services`
- 任务编排
- 执行流程控制
- 日志与状态汇总

3. `domain_plugins`
- 每个领域的规则、查询规划、清洗规则

4. `connectors`
- 具体数据源接入
- 只负责“怎么取、怎么解析”

5. `fetching`
- 基础反爬和统一网络访问层

6. `storage / writers`
- 原始层、标准化层写入
- SQLite 等输出 writer

关键原则：

- 连接器不直接操作数据库
- 连接器不自行维护重试和限速
- 领域插件不直接发 HTTP 请求
- CLI 不直接编排所有逻辑
- 所有实际业务流程统一由 service 层驱动

## 4. 项目目录结构

建议第一版目录结构如下：

```text
project_root/
  pyproject.toml
  README.md
  .env.example
  configs/
    app.yaml
    logging.yaml
    domains/
      finance.yaml
      jobs.yaml
      company_intel.yaml
      public_sentiment.yaml
    sources/
      finance/
      jobs/
      company_intel/
      public_sentiment/
    templates/
      investment_basic.yaml
      job_hunt_engineering_basic.yaml
      company_due_diligence.yaml
      job_company_reputation_check.yaml
  src/
    app/
      interfaces/
        cli/
          main.py
      services/
        task_service.py
        execution_service.py
        planning_service.py
        quality_service.py
      domain_plugins/
        base.py
        registry.py
        finance/
          plugin.py
          schema.py
          normalize.py
          dedupe.py
          topics.py
        jobs/
          plugin.py
          schema.py
          normalize.py
          dedupe.py
          topics.py
        company_intel/
          plugin.py
          schema.py
          normalize.py
          dedupe.py
          topics.py
        public_sentiment/
          plugin.py
          schema.py
          normalize.py
          dedupe.py
          topics.py
      connectors/
        base.py
        registry.py
        rss.py
        http_html.py
        json_api.py
      fetching/
        client.py
        models.py
        limiter.py
        scheduler.py
        retry.py
        identity.py
        cache.py
        robots.py
      storage/
        raw_store.py
        normalized_store.py
        artifact_store.py
        paths.py
      writers/
        base.py
        sqlite_writer.py
      models/
        task_spec.py
        policy.py
        records.py
        source_profile.py
        task_run.py
      utils/
        time.py
        hashing.py
        text.py
        jsonl.py
        compression.py
        ids.py
      reporting/
        quality_report.py
        run_report.py
  data/
    raw/
    normalized/
    artifacts/
    cache/
  tasks/
    examples/
  tests/
    unit/
    integration/
    e2e/
  docs/
    requirements/
    design/
```

## 5. 核心对象设计

### 5.1 TaskSpec

`TaskSpec` 表示一次完整任务输入，是系统的入口对象。

建议字段：

```python
class TaskSpec(BaseModel):
    task_id: str
    scenario_template: str | None
    domain: str
    targets: list[dict]
    topic_scope: list[str]
    time_range: TimeRange
    geo_scope: GeoScope | None
    source_policy: SourcePolicy
    relevance_policy: RelevancePolicy
    quality_policy: QualityPolicy
    output_policy: OutputPolicy
    run_policy: RunPolicy
```

约束：

- `domain` 只允许一个
- `targets` 不能为空
- `public_sentiment` 必须同时包含 `targets + topic_scope + time_range`
- `output_policy` 第一版默认输出 SQLite

### 5.2 CollectionPolicy

`CollectionPolicy` 是任务执行时真正使用的规则对象。它可以由：

- 任务 JSON 直接提供
- 使用场景模板预填充
- 领域插件补默认值

生成顺序建议：

1. 读取任务 JSON
2. 应用场景模板默认值
3. 应用领域插件默认值
4. 执行规则校验
5. 输出最终 `CollectionPolicy`

### 5.3 DomainPlugin

每个领域必须实现统一接口。建议：

```python
class DomainPlugin(Protocol):
    name: str

    def validate_task(self, task: TaskSpec) -> None: ...
    def build_queries(self, task: TaskSpec) -> list[PlannedQuery]: ...
    def select_sources(self, task: TaskSpec) -> list[SourceProfile]: ...
    def normalize(self, raw: RawRecord) -> list[NormalizedRecord]: ...
    def build_dedupe_key(self, record: NormalizedRecord) -> str: ...
    def output_schema(self) -> OutputSchema: ...
```

设计原则：

- 主流程只依赖接口，不依赖具体领域
- 新领域通过注册表接入
- 领域插件只处理领域规则，不直接关心 SQLite 写入

### 5.4 SourceProfile

每个数据源用 `SourceProfile` 描述。

建议字段：

- `source_id`
- `domain`
- `fetch_mode`
- `base_url`
- `rate_limit_qps`
- `max_concurrency`
- `timeout_seconds`
- `retry_policy`
- `headers_template`
- `supports_cache`
- `supports_conditional_request`
- `requires_browser`
- `robots_policy`
- `allowed_topics`
- `parser_type`

说明：

- `SourceProfile` 是连接器和 FetchClient 之间的契约
- 基础反爬策略统一放在这里配置

### 5.5 RawRecord

采集后先落 `RawRecord`，不要直接进入清洗数据库。

建议字段：

- `task_id`
- `domain`
- `source_id`
- `source_type`
- `fetch_mode`
- `fetched_at`
- `request_url`
- `final_url`
- `http_status`
- `content_hash`
- `title`
- `summary`
- `raw_payload`
- `raw_text`
- `metadata`

### 5.6 NormalizedRecord

清洗后落标准对象，作为内部标准真相。

建议字段：

- `task_id`
- `domain`
- `record_id`
- `dedupe_key`
- `source_id`
- `source_url`
- `published_at`
- `collected_at`
- `primary_entity`
- `entity_tags`
- `topic_tags`
- `title`
- `content_text`
- `language`
- `geo`
- `metrics`
- `quality_flags`
- `relevance_score`
- `extra`

说明：

- 各领域的专属字段建议放入 `extra` 或拆到领域 schema
- writer 再把统一对象映射成具体表结构

## 6. 三层数据持久化设计

### 6.1 路径规范

建议目录组织：

```text
data/
  raw/
    {domain}/
      {task_id}/
        source_{source_id}.jsonl.gz
  normalized/
    {domain}/
      {task_id}/
        normalized.jsonl.gz
  artifacts/
    {domain}/
      {task_id}/
        result.sqlite
        quality_report.json
        run_report.json
        logs/
```

### 6.2 RawStore

职责：

- 将 `RawRecord` 按源分文件写入
- 使用 `jsonl.gz`
- 尽量流式写，不一次性积攒大对象

要求：

- 文件写入幂等
- 支持追加写
- 支持内容哈希去重索引

### 6.3 NormalizedStore

职责：

- 保存清洗后的 `NormalizedRecord`
- 作为 writer 输入源之一
- 支持重新生成 SQLite，而不重跑抓取

建议：

- 采用 `jsonl.gz`
- 每行一条标准对象
- 输出前先在内存或轻量索引中做本任务内去重

### 6.4 ArtifactStore

职责：

- 统一管理 SQLite、报告、日志等产物路径
- 为后续 Web/MCP 提供统一查找入口

## 7. SQLite 输出设计

### 7.1 设计原则

SQLite 是第一版对外输出，不是内部唯一数据真相。

表结构应遵循：

- 可读
- 可查
- 可验证
- 少而清晰

### 7.2 建议基础表

#### `task_runs`

字段建议：

- `task_id`
- `domain`
- `scenario_template`
- `started_at`
- `finished_at`
- `status`
- `raw_count`
- `normalized_count`
- `output_count`
- `artifact_path`

#### `task_errors`

字段建议：

- `id`
- `task_id`
- `source_id`
- `stage`
- `error_type`
- `message`
- `detail_json`
- `created_at`

#### `raw_index`

用途：记录 raw 层索引，不存整个 raw 内容。

字段建议：

- `task_id`
- `source_id`
- `request_url`
- `content_hash`
- `fetched_at`
- `raw_file_path`

#### `quality_reports`

字段建议：

- `task_id`
- `domain`
- `report_json`
- `created_at`

### 7.3 建议领域输出表

#### finance

- `core_finance_instruments`
- `core_finance_events`
- `core_finance_metrics`

#### jobs

- `core_jobs_postings`
- `core_jobs_skills`

#### company_intel

- `core_company_profiles`
- `core_company_events`
- `core_company_projects`

#### public_sentiment

- `core_sentiment_posts`
- `core_sentiment_topics`

原则：

- 第一版宁可少建表，也不要过度范式化
- 以“容易查询和验证”为先

### 7.4 Writer 接口

建议抽象：

```python
class Writer(Protocol):
    name: str
    def write_run_metadata(self, run: TaskRun) -> None: ...
    def write_records(self, domain: str, records: Iterable[NormalizedRecord]) -> None: ...
    def finalize(self) -> Path: ...
```

第一版实现：

- `SQLiteWriter`

后续扩展：

- `PostgresWriter`
- `MongoWriter`
- `SqlScriptWriter`

## 8. 基础反爬模块设计

这部分是第一版核心之一，建议单独建完整子模块。

### 8.1 FetchClient

职责：

- 所有请求统一入口
- 按 `SourceProfile` 应用策略
- 输出统一 `FetchResponse`

建议接口：

```python
class FetchClient:
    async def fetch(self, request: FetchRequest, profile: SourceProfile) -> FetchResponse: ...
```

必须内置：

- 超时
- 重试
- 指数退避
- 随机抖动
- 状态码分类
- 压缩支持
- 条件请求支持
- 请求日志

### 8.2 RetryPolicy

建议按错误类型分层：

- 网络错误：可重试
- 429：可退避重试
- 5xx：可重试
- 4xx 非 429：默认不重试
- 解析失败：默认不通过 fetch 重试

### 8.3 RateLimiter

建议按 `source_id` 限流，而不是全局一个限流器。

建议支持：

- `qps`
- `burst`
- `max_concurrency`
- 暂停恢复

第一版可以用内存版实现，不需要 Redis。

### 8.4 Scheduler

职责：

- 将 `PlannedQuery` 拆成具体抓取任务
- 按源调度
- 尊重限速和重试策略

说明：

- 对第一版，Scheduler 不需要很复杂
- 重点是“每源隔离”和“出错可控”

### 8.5 IdentityPolicy

建议支持：

- UA 池
- 标准头模板
- Cookie 容器
- Referer 注入策略
- 代理配置占位

说明：

- 第一版不要做浏览器指纹模拟
- 只做工程稳定性能力

### 8.6 CachePolicy

建议：

- 只对幂等 GET 请求做缓存
- 结合 `ETag / Last-Modified`
- 数据存 `data/cache/`

收益：

- 节省请求量
- 降低被限频风险
- 提高重复调试效率

### 8.7 robots 与条款

建议：

- 配置层支持是否检查 robots
- `SourceProfile` 里记录数据源的访问备注
- 第一版至少保留显式开关和日志

## 9. 连接器设计

### 9.1 连接器职责边界

连接器只负责：

- 接收查询计划
- 发起抓取请求
- 解析响应
- 输出 `RawRecord`

连接器不负责：

- SQLite 写入
- 限速策略细节
- 任务级日志汇总
- 领域清洗规则

### 9.2 连接器基类

建议：

```python
class Connector(Protocol):
    source_id: str
    async def collect(self, query: PlannedQuery, ctx: ConnectorContext) -> AsyncIterator[RawRecord]: ...
```

### 9.3 第一版连接器类型

建议先实现三类通用连接器基座：

- `JsonApiConnector`
- `HtmlListDetailConnector`
- `RssConnector`

后续具体数据源只需要继承并补：

- URL 构造
- 列表页解析
- 详情页解析
- 字段映射

这样可以减少重复代码。

## 10. 领域插件设计

### 10.1 finance 插件

第一版建议支持的主题：

- `market_quote`
- `company_announcement`
- `financial_report`
- `fund_holding`
- `investment_news`

优先字段：

- 标的代码
- 标的名称
- 市场
- 指标名
- 指标值
- 币种
- 公告类型
- 发布时间
- 来源

### 10.2 jobs 插件

第一版建议支持的主题：

- `backend`
- `frontend`
- `algorithm`
- `testing`
- `devops`
- `data_engineering`
- `client`

优先字段：

- 岗位名
- 公司名
- 城市
- 薪资
- 经验要求
- 学历要求
- 技能标签
- 发布时间
- 来源链接

### 10.3 company_intel 插件

第一版建议支持的主题：

- `company_profile`
- `product_update`
- `tech_blog`
- `open_source_activity`
- `funding_event`
- `career_page`

优先字段：

- 公司名
- 产品名
- 事件类型
- 发布时间
- 关联链接
- 开源仓库信息
- 标签

### 10.4 public_sentiment 插件

第一版建议支持的主题：

- `interview_experience`
- `salary_benefits`
- `workload_overtime`
- `management_culture`
- `tech_stack_engineering`
- `layoff_hiring_freeze`
- `remote_office_policy`

优先字段：

- 目标实体
- 主题标签
- 标题
- 正文
- 发布时间
- 平台
- URL
- 互动指标
- 相关性分数

## 11. 任务执行链路

建议完整链路如下：

1. CLI 读取任务 JSON
2. 解析为 `TaskSpec`
3. 应用 `scenario_template`
4. 加载对应 `DomainPlugin`
5. 校验任务合法性
6. 选择数据源
7. 生成查询计划 `PlannedQuery`
8. Scheduler 分发到连接器
9. 连接器通过 FetchClient 抓取
10. 生成 `RawRecord`
11. 写入 RawStore
12. 领域插件做 normalize
13. 生成 `NormalizedRecord`
14. 去重与质量校验
15. 写入 NormalizedStore
16. 调用 Writer 输出 SQLite
17. 生成 `quality_report.json`
18. 生成 `run_report.json`
19. 更新任务状态

## 12. 使用场景模板设计

建议模板采用 YAML 或 JSON，保存在 `configs/templates/`。

每个模板只提供默认值，不绕过任务校验。

### 12.1 investment_basic

默认策略建议：

- 领域默认 `finance`
- 来源偏官方公告、主流投资资讯、公开基金信息
- 默认 topic 包含公告、财报、资讯
- 默认质量策略偏严格

### 12.2 job_hunt_engineering_basic

默认策略建议：

- 领域默认 `jobs`
- 地区和经验必填
- 技能标签输出开启
- 薪资标准化开启

### 12.3 company_due_diligence

默认策略建议：

- 领域默认 `company_intel`
- 来源优先官网、技术博客、开源平台、公开融资信息
- 强调公司实体命中

### 12.4 job_company_reputation_check

默认策略建议：

- 领域默认 `public_sentiment`
- 主题限制为求职相关舆情
- 相关性阈值更高
- 默认必须命中实体 + 主题共现

## 13. 输入 JSON Schema 设计草案

建议第一版任务示例如下：

```json
{
  "task_id": "job-sample-001",
  "scenario_template": "job_hunt_engineering_basic",
  "domain": "jobs",
  "targets": [
    {
      "type": "keyword",
      "value": "python 后端"
    }
  ],
  "topic_scope": ["backend"],
  "time_range": {
    "start": "2026-02-01",
    "end": "2026-03-18",
    "timezone": "Asia/Shanghai"
  },
  "geo_scope": {
    "country": "CN",
    "cities": ["Shanghai", "Hangzhou"]
  },
  "source_policy": {
    "whitelist": [],
    "blacklist": [],
    "prefer_official": false,
    "allow_rss": true,
    "allow_api": true,
    "allow_html": true,
    "max_sources": 5
  },
  "relevance_policy": {
    "require_target_match": true,
    "require_topic_match": true,
    "require_cooccurrence": false,
    "min_relevance_score": 0.6
  },
  "quality_policy": {
    "required_fields": ["title", "source_url", "published_at"],
    "dedupe_mode": "strict",
    "max_missing_ratio": 0.2
  },
  "output_policy": {
    "writer": "sqlite",
    "keep_raw": true,
    "keep_normalized": true,
    "max_output_records": 2000,
    "update_mode": "replace"
  },
  "run_policy": {
    "max_concurrency": 8,
    "timeout_seconds": 20,
    "retry_times": 3,
    "enable_cache": true,
    "allow_browser": false
  }
}
```

## 14. 质量报告设计

建议输出 `quality_report.json`，至少包含：

- `task_id`
- `domain`
- `raw_count`
- `normalized_count`
- `deduped_count`
- `output_count`
- `dropped_by_relevance`
- `dropped_by_quality`
- `missing_field_stats`
- `source_stats`
- `error_stats`
- `warnings`

目的：

- 帮助验证任务是否正确
- 帮助发现数据源质量问题
- 帮助决定是否要调整规则

## 15. 日志与运行状态设计

建议记录三类日志：

- 任务级日志
- 数据源级日志
- 请求级日志

第一版不必接入复杂日志系统，但建议：

- 控制台输出简明摘要
- 文件保留详细日志
- 错误对象统一结构化

运行状态建议：

- `pending`
- `running`
- `partial_success`
- `success`
- `failed`

## 16. 测试策略

### 16.1 单元测试

覆盖：

- schema 校验
- 模板合并
- 领域规则校验
- 去重键生成
- 标准化函数
- 质量报告生成

### 16.2 集成测试

覆盖：

- FetchClient 与限流器
- 通用连接器
- RawStore / NormalizedStore
- SQLiteWriter

### 16.3 端到端测试

每个领域至少一个样例任务：

- 输入任务 JSON
- 跑完整流程
- 断言产出 raw 文件
- 断言产出 normalized 文件
- 断言 SQLite 表存在且有记录

## 17. 第一版建议实施顺序

建议采用最小闭环优先，而不是四个领域同时展开。

### 阶段 1：基础骨架

先完成：

- 项目目录
- 配置加载
- `TaskSpec` 和策略模型
- `DomainPlugin` 接口
- `Writer` 接口
- 三层存储路径管理
- CLI 骨架

### 阶段 2：基础反爬基础设施

完成：

- `FetchClient`
- `RetryPolicy`
- `RateLimiter`
- `Scheduler`
- `IdentityPolicy`
- 简单缓存

### 阶段 3：先打通一个领域最小闭环

我建议第一版最先打通 `jobs`。

原因：

- 结构比 finance 更直观
- 字段验证容易
- 对你“找工作”目标直接有价值
- 便于先验证整体架构

打通内容：

- 1 到 2 个公开数据源
- 任务输入 -> raw -> normalized -> SQLite
- 质量报告生成

### 阶段 4：补 company_intel

原因：

- 同时服务求职和投资
- 与 jobs 配合价值高
- 结构相对可控

### 阶段 5：补 finance

原因：

- 字段复杂度高
- 数据源差异大
- 适合在框架稳定后再补

### 阶段 6：补 public_sentiment
原因：

- 过滤噪声和相关性规则最容易变复杂
- 应在前三个领域稳定后接入

### 阶段 7：完善 CLI 和报告

完成：

- 任务列表
- 任务状态查看
- 结果路径输出
- 日志查看辅助命令

## 18. 后续扩展路径

### 18.1 Web

在已有 service 层基础上增加 HTTP 接口与前端页面，不改动领域逻辑和 writer。

### 18.2 MCP

将 service 层封装成工具接口，例如：

- `submit_task`
- `get_task_status`
- `get_quality_report`
- `list_artifacts`

### 18.3 skill

skill 基于 MCP 工具描述：

- 何时使用哪个模板
- 如何生成 JSON 任务
- 如何解读输出结果

## 19. 实现阶段需要尽快确认的具体项

虽然总体方案已定，但编码前最好尽快确认以下实现细节：

1. 第一版首个落地领域是否按建议从 `jobs` 开始
2. 每个领域第一批优先数据源名单
3. 压缩格式优先用 `gzip` 还是 `zstd`
4. SQLite 是否允许存部分长文本正文
5. CLI 命令风格是否采用：
   - `run`
   - `validate`
   - `inspect`
   - `report`

## 20. 结论

第一版实现建议总结如下：

- 语言：Python 3.12
- 入口：CLI
- 内部数据真相：`raw jsonl.gz + normalized jsonl.gz`
- 对外输出：`SQLite`
- 架构核心：`DomainPlugin + Connector + FetchClient + Writer`
- 核心基础设施：工程级基础反爬
- 扩展方式：领域、数据源、模板、writer 全部插件化
- 落地顺序：先骨架，再反爬，再单领域闭环，再逐领域扩展

该方案与需求文档 v0.1 保持一致，适合直接进入编码阶段。

