# 结构化需求驱动的数据采集、清洗与数据库输出项目需求文档 v0.1

## 1. 文档目的

本文档用于定义第一版项目需求边界、核心能力、数据规则、系统分层和验收标准。

本项目目标不是构建一个无限制的通用爬虫，而是构建一个面向实际决策场景的、规则驱动的定向数据获取系统。第一版优先服务以下两类使用场景：

- 投资相关信息获取
- 程序员求职相关信息获取

项目首期只实现本地 CLI 触发的基本功能，不接入大模型推理，不实现 skill 和 MCP server 本体，但架构必须为后续接入留出清晰扩展接口。

## 2. 项目目标

第一版系统应具备以下能力：

- 接收结构化任务输入
- 根据任务规则从公开可访问网络数据源定向获取数据
- 对采集结果进行标准化、清洗、去重和质量校验
- 输出为一个易于验证正确性的数据库结果
- 同时保留原始数据和清洗后中间数据，支持后续重放和重新输出

第一版明确采用以下原则：

- 只处理公开可访问数据源
- 只支持单次手动触发任务
- 一次任务只处理一个一级领域
- 不使用大模型参与采集、清洗和决策
- 不做复杂机器学习建模
- 不做分布式处理
- 不做流式处理
- 只做工程级基础反爬，不做重型对抗式反爬

## 3. 第一版范围

### 3.1 纳入范围

- 本地 CLI 触发任务
- 结构化 JSON 输入
- 定向采集规则执行
- 公开网页、公开 API、RSS/Atom 等数据源接入
- 基础反爬基础设施
- 原始层、标准化层、输出层三层数据持久化
- SQLite 输出
- 任务日志、错误日志、质量报告
- 可扩展领域插件机制
- 可扩展输出 writer 机制

### 3.2 暂不纳入范围

- 付费数据源正式接入
- 需要登录的数据源正式接入
- 复杂浏览器指纹对抗
- 人工验证码处理
- 分布式调度
- 实时流式采集
- 大模型驱动的需求理解
- 机器学习情绪模型、分类模型、排序模型
- Web 页面
- MCP server
- skill 输出

## 4. 核心设计原则

### 4.1 定向采集原则

所有领域均采用定向采集，不允许无边界泛抓。

每次任务必须至少明确以下信息中的大部分：

- 目标对象
- 主题范围
- 时间范围
- 来源范围
- 输出范围

### 4.2 内部存储无关原则

数据库仅作为对外输出形态之一，不作为系统内部唯一数据真相。

系统内部采用三层持久化：

- `raw`：原始结构化记录
- `normalized`：清洗后的规范化记录
- `artifacts`：数据库输出、质量报告、日志等产物

### 4.3 可扩展原则

以下能力必须插件化或抽象化：

- 一级领域
- 数据源连接器
- 输出目标
- 使用场景模板
- 反爬策略配置

### 4.4 可验证性优先原则

第一版以“易验证正确性”为优先目标，因此主输出选择 `SQLite`。

## 5. 使用场景与领域模型

### 5.1 使用场景层

项目当前主要服务两个使用场景：

- 投资
- 求职

### 5.2 数据领域层

领域需要可扩展。第一版内置四个一级领域：

- `finance`
- `jobs`
- `company_intel`
- `public_sentiment`

### 5.3 领域与使用场景关系

- 投资场景主要依赖：
  - `finance`
  - `company_intel`
  - `public_sentiment`
- 求职场景主要依赖：
  - `jobs`
  - `company_intel`
  - `public_sentiment`

其中 `public_sentiment` 在求职场景中属于辅助域，不与 `jobs` 强绑定。

### 5.4 后续扩展

后续可新增领域，例如：

- `macro`
- `salary_benchmark`
- `startup`
- `education`

系统设计不得假设只有当前四个领域存在。

## 6. 第一版领域定义

### 6.1 finance

用于获取投资相关信息，范围包括但不限于：

- 股票
- 基金
- ETF
- 公司公告
- 财务指标
- 估值相关信息
- 持仓相关信息
- 宏观数据
- 投资资讯

要求：

- 必须围绕明确标的、明确主题或明确市场范围采集
- 不做无目标全市场无限制抓取

### 6.2 jobs

用于获取程序员求职相关岗位信息，第一版重点关注：

- 后端
- 前端
- 算法
- 测试
- DevOps
- 数据开发
- 客户端

要求：

- 必须围绕岗位关键词、地区、经验层级等条件定向采集
- 第一版重点服务程序员岗位，不追求覆盖全部职业

### 6.3 company_intel

用于获取公司情报相关信息，服务投资和求职两个场景。范围包括：

- 公司基础信息
- 产品动态
- 技术博客
- 开源项目活动
- 融资相关信息
- 招聘官网信息
- 团队或组织变化

说明：

- 该领域用于替代过于宽泛的“科技”概念
- 第一版从实现和规则上按“公司情报”设计

### 6.4 public_sentiment

用于获取公开可访问的公众讨论数据，但必须是主题受限采集，不允许泛化抓取全网情绪。

第一版仅支持定向采集以下类型的舆情：

- 公司相关讨论
- 产品相关讨论
- 面试体验
- 薪资福利
- 工作强度
- 管理风格
- 技术栈讨论
- 裁员或招聘冻结讨论

要求：

- 必须带目标实体
- 必须带主题范围
- 必须带时间范围
- 必须可配置来源白名单或来源范围

## 7. 结构化输入需求

第一版输入为 JSON，不支持自然语言输入转结构化任务。

### 7.1 顶层输入字段建议

- `scenario_template`
- `domain`
- `targets`
- `topic_scope`
- `time_range`
- `geo_scope`
- `source_policy`
- `relevance_policy`
- `quality_policy`
- `output_policy`
- `run_policy`

### 7.2 字段说明

#### `scenario_template`

表示预设使用场景模板，例如：

- `investment_basic`
- `job_hunt_engineering_basic`
- `company_due_diligence`
- `job_company_reputation_check`

#### `domain`

本次任务对应的一级领域，只允许一个值。

#### `targets`

目标对象集合，按领域不同可包括：

- 股票代码
- 基金代码
- 公司名
- 岗位关键词
- 产品名
- 技术主题

#### `topic_scope`

限定采集主题范围。不同领域使用不同的主题集合。

#### `time_range`

时间范围对象，应至少支持：

- `start`
- `end`
- `timezone`

#### `geo_scope`

地理范围，适用于职位、公司和部分舆情场景。

#### `source_policy`

来源策略，应支持：

- 白名单
- 黑名单
- 官方优先
- 最大来源数
- 是否允许 RSS
- 是否允许 API
- 是否允许网页抓取

#### `relevance_policy`

相关性规则，应支持：

- 是否必须命中目标实体
- 是否必须命中主题词
- 最低相关性分数
- 是否需要实体与主题共现
- 最大噪声容忍度

#### `quality_policy`

质量规则，应支持：

- 必填字段
- 去重规则
- 缺失容忍度
- 异常值处理策略
- 最小有效记录数要求

#### `output_policy`

输出规则，应支持：

- 输出格式
- 是否保留原始层
- 是否保留标准化层
- 最大输出记录数
- 更新方式

#### `run_policy`

运行规则，应支持：

- 并发数
- 超时
- 重试次数
- 是否启用缓存
- 是否允许浏览器模式

## 8. 采集规则设计

项目采用统一采集规则模型，而不是为每个领域发明完全不同的流程。

### 8.1 通用采集规则模型

定义统一规则对象 `CollectionPolicy`，包含：

- 目标对象规则
- 主题规则
- 时间规则
- 来源规则
- 相关性规则
- 质量规则
- 输出规则

### 8.2 领域策略模型

每个领域通过 `DomainPolicy` 提供领域专属能力，至少包括：

- `input_schema`
- `source_catalog`
- `query_builder`
- `relevance_rules`
- `normalization_rules`
- `dedupe_rules`
- `output_schema`

### 8.3 使用场景模板

项目需要支持“场景模板”对采集规则进行预填充。模板本身不替代领域，而是在领域之上提供默认规则组合。

例如：

- `investment_basic`
  - 默认来源偏官方和主流资讯
  - 默认关注公告、财务、行情、资讯
- `job_hunt_engineering_basic`
  - 默认关注程序员岗位
  - 默认字段包含薪资、城市、经验、技能
- `company_due_diligence`
  - 默认关注公司官网、产品动态、技术博客、融资信息
- `job_company_reputation_check`
  - 默认使用 `public_sentiment`
  - 默认主题限制为面试、加班、福利、管理风格

## 9. public_sentiment 的主题受限采集要求

### 9.1 设计原则

`public_sentiment` 必须是主题受限采集域，不允许无主题、无实体地全网抓取。

### 9.2 任务约束

此领域的任务应至少包含：

- 目标实体
- 主题范围
- 时间范围
- 来源范围

### 9.3 求职场景中的使用方式

在求职场景中，`public_sentiment` 仅作为辅助域，主要用于补充如下信息：

- 面试体验
- 薪资福利讨论
- 工作强度讨论
- 管理风格讨论
- 技术栈或工程文化讨论

### 9.4 第一版主题词体系建议

第一版建议内置以下主题标签：

- `interview_experience`
- `salary_benefits`
- `workload_overtime`
- `management_culture`
- `tech_stack_engineering`
- `layoff_hiring_freeze`
- `remote_office_policy`

### 9.5 第一版实现方式

第一版不用大模型，采用规则链实现：

- 根据目标实体和主题范围生成查询词
- 用来源策略限制搜索范围
- 用标题和摘要做预过滤
- 抽取正文后再做实体与主题共现过滤
- 基于词典、词距、规则打标签
- 为每条记录计算相关性分数
- 低分记录不进入最终输出

## 10. 数据源需求

### 10.1 第一版支持的数据源类型

- 公开 API
- 公开网页
- RSS/Atom
- 官方公告页
- 公司官网
- 招聘网站公开页面
- 技术博客
- 开源平台公开页
- 公开论坛或公开评论页

### 10.2 每个数据源应具备的配置

- 数据源名称
- 所属领域
- 访问模式
- 基础 URL
- 频率限制
- 默认请求头
- 支持的查询能力
- 解析方式
- 是否允许缓存
- 是否支持条件请求
- 法律或使用条款备注

### 10.3 付费和登录源的预留设计

第一版不接入，但架构需预留以下能力：

- `AuthProvider`
  - API Key
  - OAuth
  - Cookie/Session
  - 用户名密码登录
- `SourceAccessPolicy`
  - 是否需要登录
  - 是否需要授权
  - 访问频控规则
  - 条款说明

## 11. 工程级基础反爬需求

基础反爬是本项目第一版核心基础设施之一。

第一版目标不是做强对抗，而是做稳定、可扩展、对个人和小团队都适用的抓取基础层。

### 11.1 SourceProfile

每个数据源必须通过 `SourceProfile` 定义抓取策略，至少包括：

- 访问方式
- 并发上限
- 限速策略
- 重试策略
- 超时
- 是否允许缓存
- 是否允许条件请求
- 是否需要浏览器模式
- 默认请求头模板
- 条款检查开关

### 11.2 FetchClient

所有连接器统一通过 `FetchClient` 发起网络请求，不允许各连接器自行随意发请求。

`FetchClient` 至少应支持：

- 请求超时
- 自动重试
- 指数退避
- 随机抖动
- 连接池
- 压缩响应支持
- 条件请求
- 响应状态分类
- 错误分类
- 请求日志记录

### 11.3 RateLimiter 与 Scheduler

应按数据源分别做限速和调度，至少支持：

- 每源并发控制
- 每源请求速率限制
- 失败熔断
- 暂停和恢复
- 任务级最大请求数控制

### 11.4 IdentityPolicy

第一版应支持基础身份策略：

- User-Agent 池
- 常用请求头模板
- Accept-Language 模板
- Referer 策略
- Cookie 容器
- 代理接口预留

### 11.5 FetchMode

数据源抓取模式应抽象为：

- `api`
- `http`
- `rss`
- `browser`

第一版重点实现：

- `api`
- `http`
- `rss`

`browser` 仅预留接口，后续视数据源需要再接。

## 12. 数据清洗需求

### 12.1 通用清洗目标

- 统一字段命名
- 统一类型
- 统一时间格式
- 标准化实体字段
- 降低噪声
- 识别重复记录
- 构造稳定主键或业务键

### 12.2 通用清洗步骤

- 原始字段映射
- 类型转换
- HTML 清理
- 正文提取
- 时间标准化
- 地理信息标准化
- 缺失值处理
- 去重键生成
- 来源信息补充
- 抓取时间补充

### 12.3 领域专属清洗

#### finance

- 证券代码标准化
- 市场标识标准化
- 币种标准化
- 指标映射
- 公告类型归一化

#### jobs

- 岗位名称归一化
- 技能标签提取
- 薪资标准化
- 经验与学历标准化
- 城市层级标准化

#### company_intel

- 公司实体归一化
- 产品名归一化
- 事件类型归类
- 开源项目与公司关联

#### public_sentiment

- 文本清理
- 实体标签识别
- 主题标签识别
- 相关性评分
- 低质量内容过滤

### 12.4 质量校验

每个领域应至少支持：

- 必填字段校验
- 枚举值校验
- 时间合法性校验
- 唯一性校验
- 异常值检查
- 清洗前后记录数统计

## 13. 数据持久化设计

### 13.1 三层持久化

项目内部持久化分为三层：

- 原始层 `raw`
- 标准化层 `normalized`
- 输出层 `artifacts`

### 13.2 原始层

用于保存采集后结构化但未清洗的数据。

建议格式：

- `jsonl.gz`

保存内容至少包括：

- 原始字段
- 来源
- 抓取时间
- URL
- 响应状态
- 内容哈希

### 13.3 标准化层

用于保存清洗、标准化、打标签后的规范化记录。

建议格式：

- `jsonl.gz`

保存内容至少包括：

- 领域标识
- 标准字段
- 去重键
- 相关性分数
- 数据质量标记
- 来源信息
- 抓取时间

### 13.4 输出层

用于保存对外可交付产物。

第一版至少包括：

- SQLite 数据库
- `quality_report.json`
- 任务日志

## 14. 原始数据保留策略

为控制存储容量，采用分层保留策略。

### 14.1 L0 元数据层

长期保留以下元数据：

- URL
- 来源
- 抓取时间
- 内容哈希
- 响应状态
- 任务 ID

### 14.2 L1 原始结构化记录层

长期保留去重后的原始结构化记录，使用压缩格式存储。

### 14.3 L2 原始页面层

原始 HTML 或响应体只做短期保留，用于调试和失败排查。

建议：

- 默认关闭长期保存
- 可配置保留 7 天或 30 天
- 失败样本优先保留

### 14.4 容量控制机制

应支持：

- 内容哈希去重
- 任务归档
- 来源归档
- TTL 清理策略
- 大文本可选裁剪或摘要化

## 15. 输出设计

### 15.1 第一版主输出

第一版主输出确定为 `SQLite`。

选择理由：

- 易验证正确性
- 无需外部服务
- 便于本地查询与调试
- 约束和索引更易暴露数据问题

### 15.2 第一版输出原则

项目内部必须存储无关，SQLite 只是第一版 writer。

后续应可扩展：

- `SQLiteWriter`
- `PostgresWriter`
- `MongoWriter`
- `SqlScriptWriter`

### 15.3 SQLite 建议结构

应至少包括：

- `task_runs`
- `task_errors`
- `raw_index`
- 各领域 `core_*` 表

根据需要可增加：

- `quality_reports`
- `source_runs`

## 16. 系统架构需求

### 16.1 核心模块

- 输入解析器
- 规则校验器
- 场景模板管理器
- 任务编排器
- 领域插件管理器
- 数据源连接器
- 统一抓取客户端
- 清洗引擎
- 质量校验器
- Writer 输出器
- 日志模块

### 16.2 分层建议

- `domain_plugins`
- `connectors`
- `fetching`
- `normalization`
- `writers`
- `storage`
- `services`
- `interfaces`

### 16.3 接口层要求

第一版只实现：

- `interfaces/cli`

后续保留：

- `interfaces/web`
- `interfaces/mcp`

注意：

- CLI、Web、MCP 都只应作为入口
- 实际业务逻辑必须放在 service 层

## 17. 领域扩展机制

领域不得写死为固定 if/else 流程。

应抽象为 `DomainPlugin`，至少提供：

- 领域名称
- 输入 schema
- 默认模板兼容关系
- 数据源目录
- 查询规划逻辑
- 解析逻辑
- 清洗逻辑
- 去重逻辑
- 输出 schema

后续新增或替换领域时，不应要求重写主流程。

## 18. 浏览器页面与后续扩展

### 18.1 Web 页面

第一版不实现浏览器页面，但应为后续页面能力预留接口。

后续页面可支持：

- 浏览任务
- 手动触发任务
- 查看日志
- 查看质量报告
- 下载 SQLite 结果

### 18.2 MCP server

后续可将核心能力暴露为 MCP 工具，例如：

- 提交任务
- 查询任务状态
- 查看日志
- 查询结果摘要
- 导出产物

### 18.3 skill

后续 skill 的作用是让大模型学会：

- 选择场景模板
- 生成任务 JSON
- 调用 MCP 工具
- 理解输出结果

因此 skill 依赖核心项目和 MCP，不应在第一版先做。

## 19. 关于复杂反爬、机器学习、分布式和流式处理的定位

### 19.1 复杂反爬

适用位置：

- FetchClient
- 浏览器抓取模块
- 代理管理
- 请求身份管理

建议：

- 第一版仅实现基础版
- 后续按具体数据源需要追加浏览器能力和更复杂策略

### 19.2 机器学习模型

适用位置：

- 情绪分类
- 主题分类
- 实体对齐
- 相似去重
- 异常检测

建议：

- 第一版不使用
- 后续可以把本项目输出数据库交给其他 agent 或下游模型系统分析

### 19.3 分布式处理

适用位置：

- 大规模历史回补
- 海量多源同步
- 高并发批处理

建议：

- 第一版不使用
- 在任务规模确实超出单机能力前不建议引入

### 19.4 流式处理

适用位置：

- 实时行情监控
- 实时舆情跟踪
- 订阅与告警

建议：

- 第一版不使用
- 后续如果项目目标转向监控系统再考虑引入

## 20. 非功能需求

### 20.1 稳定性

- 单数据源失败不应导致整任务直接崩溃
- 应支持失败重试和错误归档

### 20.2 可维护性

- 规则配置化
- 领域插件化
- 数据源配置化
- 输出 writer 抽象化

### 20.3 可观测性

- 任务日志
- 每源请求统计
- 清洗前后记录数
- 去重前后记录数
- 输出记录数
- 错误分类统计

### 20.4 合规性

- 仅抓取公开可访问数据
- 保留来源信息
- 对需登录或付费数据源只预留接口，不绕过授权

## 21. 测试与验收

### 21.1 测试类型

- 输入 schema 校验测试
- 场景模板测试
- 数据源连接测试
- 清洗规则单元测试
- 去重测试
- SQLite 输出测试
- 端到端测试

### 21.2 第一版验收标准

- 能通过 CLI 提交 JSON 任务
- 四个内置领域至少各有一个可跑通样例
- 所有任务都使用定向采集规则
- 原始层、标准化层、输出层均能生成
- SQLite 能直接查询
- 质量报告可生成
- 失败任务有可追踪日志
- 基础反爬基础设施已被统一接入连接器流程

## 22. 第一版后续实现顺序建议

建议按以下顺序推进：

1. 定义输入 schema 与场景模板
2. 定义领域插件接口和 writer 接口
3. 实现基础反爬基础设施
4. 实现三层持久化目录规范
5. 先打通一个领域的最小闭环
6. 再补齐四个内置领域
7. 完善质量报告和 CLI
8. 再进入 Web、MCP、skill 扩展

## 23. 当前确认结论

截至 v0.1，已确认如下结论：

- 第一版主输出采用 `SQLite`
- 项目内部采用存储无关设计
- 原始层和标准化层使用压缩文件持久化
- 第一版只支持公开可访问数据源
- 第一版采用工程级基础反爬
- 第一版不使用大模型和机器学习模型
- 第一版只支持本地 CLI 手动触发
- 浏览器页面后置
- skill 和 MCP 后置
- 领域需可扩展
- 第一版内置领域为：
  - `finance`
  - `jobs`
  - `company_intel`
  - `public_sentiment`
- `public_sentiment` 必须主题受限采集

## 24. 待后续实现方案阶段细化的内容

以下内容已达成方向，但需要在实现方案中进一步细化：

- 具体项目目录结构
- 输入 JSON schema 详细定义
- 各领域第一批数据源名单
- SQLite 具体表结构
- 原始层与标准化层目录命名规则
- FetchClient 的具体技术选型
- CLI 命令设计
- 第一版优先打通的领域顺序
